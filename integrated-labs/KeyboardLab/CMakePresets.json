{
  "version": 3,
  "configurePresets": [
    {
      "name": "default",
      "displayName": "Default (server) build with gcc-13",
      "binaryDir": "${sourceDir}/build",
      "condition": {
        "type": "equals",
        "lhs": "${hostSystemName}",
        "rhs": "Linux"
      },
      "cacheVariables": {
        "CMAKE_C_COMPILER": "/usr/bin/gcc-13",
        "CMAKE_CXX_COMPILER": "/usr/bin/g++-13"
      }
    },
    {
      "name": "personal-computer",
      "displayName": "Build on personal computer (system default compiler)",
      "binaryDir": "${sourceDir}/build"
    }
  ]
}
