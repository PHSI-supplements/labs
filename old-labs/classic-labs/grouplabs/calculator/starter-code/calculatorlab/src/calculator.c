#include <CowPi.h>
#include "interrupt_support.h"
#include "outputs.h"

void intialize_calculator(void) {
    // this code will run once
    ;
}

void manage_calculator(void) {
    // this code will run with every iteration of the main loop
    // (do NOT use this function as your ISR)
    ;
}